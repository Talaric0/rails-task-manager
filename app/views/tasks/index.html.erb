<h1>Here are your tasks, lazyboi...</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
  <%= check_box_tag "completed", "1", task.completed, disabled: true %> <%= link_to task.title, edit_task_path(task) %>
  <%= link_to '✖', task_path(task.id), method: :delete, data: {confirm: 'Are you sure?'} %>
  </li>
  <% end %>
</ul>

<%= link_to "Add a Task", "/tasks/new" %>